The script can be run in Termux or via adb shell directly

looks for Ironsource apps and disables them via pm uninstall --user 0 <package>, let me know if any packages/variants should be added.

Usage:
```
sh ironkansas.sh
```
